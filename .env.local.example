# Firebase - required for auth and Firestore
# Get from Firebase Console > Project Settings > General
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# Firebase Admin - for API auth (verify tokens)
# Firebase Console > Project Settings > Service Accounts > Generate new private key
# Paste the entire JSON as a single line or use JSON.stringify(parsed)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Redis (optional) - distributed rate limits for enterprise invite endpoints across multiple app instances
# Example: redis://localhost:6379 or Upstash rediss://default:TOKEN@HOST:6379
# REDIS_URL=

# Backblaze B2 - for file storage (when set, sync uses B2 instead of Firebase Storage)
# B2 Console: Application Keys > Add a New Application Key
# Bucket: Bucket Settings > S3 Endpoint (e.g. https://s3.us-west-004.backblazeb2.com)
B2_ACCESS_KEY_ID=
B2_SECRET_ACCESS_KEY=
B2_BUCKET_NAME=bizzi-cloud
B2_ENDPOINT=
B2_REGION=us-west-004

# CDN (optional) - Route B2 downloads through Cloudflare for free egress (Bandwidth Alliance)
# When set, download URLs use cdn.yourdomain.com instead of presigned B2 URLs
# Requires: Cloudflare Worker deployed (see cloudflare-worker/README.md)
# CDN_BASE_URL=https://cdn.bizzicloud.io
# CDN_SECRET=  # Same as B2_SECRET_ACCESS_KEY, or a separate secret shared with the Worker

# App-level encryption (optional) - for sensitive metadata (API tokens, recovery codes, etc.)
# Generate: openssl rand -hex 32
# When set, use encryptField/decryptField for sensitive DB fields.
# APP_ENCRYPTION_KEY_CURRENT=
# APP_ENCRYPTION_KEY_PREVIOUS=  # For key rotation
# APP_ENCRYPTION_KEY_VERSION=1

# Client session - for email-gate access (invited clients without sign-up)
# Generate: openssl rand -hex 32
# Required for /client email verification when photographers invite clients
CLIENT_SESSION_SECRET=

# Stripe - for subscriptions and billing
# Dashboard: https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_
# Webhook: Create endpoint in Stripe Dashboard pointing to /api/webhooks/stripe
# Required events (platform + Cochran Connect Express mirror):
#   checkout.session.completed, customer.subscription.created, customer.subscription.updated,
#   customer.subscription.deleted, invoice.paid, invoice.payment_failed,
#   account.updated, capability.updated
# Connect onboarding uses Account Links (Express), not OAuth — do not subscribe to account.application.deauthorized
# unless you migrate to Stripe Connect OAuth (different event model; see Stripe OAuth docs).
# Local dev: run `stripe listen --forward-to localhost:3000/api/webhooks/stripe` for whsec_
STRIPE_WEBHOOK_SECRET=whsec_

# Stripe Connect (Cochran Films Express): uses the same keys above. Enable Connect (Express) in Stripe Dashboard
# and complete platform profile. Consumer subscription checkout on /api/stripe/checkout may apply a 60/40 destination
# split when the operator profile (info@cochranfilms.com) has a ready stripe_connect_account_id.

# Stripe Price IDs (optional) - if set, these override dynamic creation
# Products/Prices are created automatically on first checkout and cached in Firestore
# Base plans: STRIPE_PRICE_SOLO_MONTHLY, STRIPE_PRICE_SOLO_ANNUAL, etc.
# Addons: STRIPE_PRICE_GALLERY, STRIPE_PRICE_EDITOR, STRIPE_PRICE_FULLFRAME

# EmailJS - for enterprise invoice, sign-up link, and file share emails
# Dashboard: https://dashboard.emailjs.com
# Enable API requests: Account > Security
EMAILJS_SERVICE_ID=
EMAILJS_TEMPLATE_ID_INVOICE=
EMAILJS_TEMPLATE_ID_SIGNUP=
# Share notification email (when user shares files/folders with another user)
EMAILJS_TEMPLATE_ID_SHARE=
# Workspace share: email to org/team admin only when share targets enterprise workspace or personal team (see email-templates/share-workspace.html)
EMAILJS_TEMPLATE_ID_SHARE_WORKSPACE=
# Cross-workspace pending delivery: moderation email to admins (see email-templates/share-workspace-delivery-request.html). If unset, SHARE_WORKSPACE is used with delivery_request=1.
EMAILJS_TEMPLATE_ID_SHARE_WORKSPACE_DELIVERY_REQUEST=
# Transfer notification email (when user creates transfer and enters client email)
EMAILJS_TEMPLATE_ID_TRANSFER=
# Subscription welcome email (when consumer purchases subscription on platform)
EMAILJS_TEMPLATE_ID_SUBSCRIPTION_WELCOME=
# Itemized receipt when user applies plan / add-on / team seat changes in-app (see email-templates/subscription-change-receipt.html).
# EmailJS template should include {{line_items_plain}} in a <pre> (or {{{line_items_html}}} with triple braces for a table).
EMAILJS_TEMPLATE_ID_SUBSCRIPTION_CHANGE_RECEIPT=
# Gallery invite email (when photographer creates invite-only gallery with invited emails)
EMAILJS_TEMPLATE_ID_GALLERY_INVITE=
# Support ticket email (when user submits support ticket from dashboard; To = your support inbox)
EMAILJS_TEMPLATE_ID_SUPPORT=
# Org removal emails (when admin removes enterprise organization; owner vs member templates)
EMAILJS_TEMPLATE_ID_ORG_REMOVAL_OWNER=
EMAILJS_TEMPLATE_ID_ORG_REMOVAL_MEMBER=
# Org seat invite (when org admin invites member by email from seat management)
EMAILJS_TEMPLATE_ID_ORG_SEAT_INVITE=
# Org lifecycle: entered recovery storage / restored / permanently purged after retention
EMAILJS_TEMPLATE_ID_ORG_RECOVERY_STORAGE=
EMAILJS_TEMPLATE_ID_ORG_RESTORED=
EMAILJS_TEMPLATE_ID_ORG_PURGED=
# Personal team lifecycle: entered recovery storage / restored / purged
EMAILJS_TEMPLATE_ID_TEAM_RECOVERY_STORAGE=
EMAILJS_TEMPLATE_ID_TEAM_RESTORED=
EMAILJS_TEMPLATE_ID_TEAM_PURGED=
# Personal team invite (personal-account team admin invites member — not Organization)
EMAILJS_TEMPLATE_ID_PERSONAL_TEAM_INVITE=
# Waitlist (`/waitlist` → HubSpot success): admin inbox + submitter recap (see email-templates/waitlist-*.html)
EMAILJS_TEMPLATE_ID_WAITLIST_ADMIN=
EMAILJS_TEMPLATE_ID_WAITLIST_CLIENT=
# Inbox that receives new waitlist submissions (must match EmailJS admin template "To" {{admin_email}})
WAITLIST_ADMIN_NOTIFY_EMAIL=
EMAILJS_PUBLIC_KEY=
EMAILJS_PRIVATE_KEY=

# Base URL - use the canonical domain that does NOT redirect (avoids 307 webhook failures)
# If bizzicloud.io redirects to www, use www. If www redirects to apex, use apex.
# NEXT_PUBLIC_APP_URL=https://www.bizzicloud.io

# --- Google Drive migration (OAuth + Picker; scope https://www.googleapis.com/auth/drive.file)
# Cloud Console: enable Google Drive API + Google Picker API; create OAuth client (web) + API key.
# MIGRATION_GOOGLE_CLIENT_ID=
# MIGRATION_GOOGLE_CLIENT_SECRET=
# Browser API key for Google Picker (same project as OAuth client):
# NEXT_PUBLIC_GOOGLE_MIGRATION_PICKER_KEY=

# --- Asset delivery (egress + Fluid): all default OFF — enable in deploy env after parity QA.
# Rollback: set each flag to false/omit it; no code deploy required.
# Firestore proxy hints: skip B2 HeadObject when backup_files says proxy is ready (Class B savings).
# DELIVERY_USE_FIRESTORE_PROXY_HINTS=true
# Break-glass verify with HeadObject when hints are on (extra Class B).
# DELIVERY_HEAD_FALLBACK=true
# Sample rate 0–1 for structured delivery_telemetry logs (stdout JSON).
# DELIVERY_LOG_SAMPLE_RATE=0.01
# List thumbnails: generate to B2 then 307 redirect to signed CDN URL; client fetches via follow+ blob URL.
# THUMBNAIL_REDIRECT_TO_CDN=true
# Consolidated POST /api/backup/asset-preview; pair with client flag.
# ASSET_PREVIEW_CONSOLIDATED=true
# NEXT_PUBLIC_ASSET_PREVIEW_CONSOLIDATED=true
# Gallery video: when proxy missing, return processing JSON instead of streaming original (safer / less egress).
# GALLERY_VIDEO_NO_ORIGINAL_FALLBACK=true
# Migration finalize: extra proxy enqueue for video-like imports.
# MIGRATION_PROXY_ENQUEUE_V2=true
# Mount metadata: proxy path/size from Firestore only (no per-row HeadObject).
# MOUNT_METADATA_USE_DB_PROXY_ONLY=true
# Client preview polling backoff cap (ms) — server hint for documentation; primary client cap in FilePreviewModal.
# PREVIEW_POLL_BACKOFF_MAX_MS=30000

# DEV ONLY: Skip Firebase Admin auth for B2 upload-url when testing without service account key
# Set to "true" ONLY in local .env.local. NEVER set in production.
# B2_SKIP_AUTH_FOR_TESTING=true

# BRAW proxy generation - path to ffmpeg-braw binary (Linux x86_64)
# Required for Blackmagic RAW (.braw) proxy creation. Run: node scripts/download-ffmpeg-braw.js
# FFMPEG_BRAW_PATH=/path/to/project/bin/ffmpeg-braw/ffmpeg
# Linux PM2 worker: POST /api/workers/braw-proxy/* uses this secret (set on Vercel + worker env).
# MEDIA_BRAW_WORKER_SECRET=
# Optional: native SDK CLI from native/braw-proxy-cli (if set, used instead of FFMPEG_BRAW_PATH on the worker).
# BRAW_PROXY_CLI_BIN=/opt/braw-worker/bin/braw-proxy-cli
# ENCODER_FFMPEG_PATH=/usr/bin/ffmpeg

# --- HubSpot: waitlist form (`/waitlist` → POST /api/hubspot/pre-register)
# Submissions send Forms API `context.hutk` from the `hubspotutk` cookie when present (links to contacts).
# The waitlist page loads js.hs-scripts.com after the user accepts Analytics in the cookie banner.
# Private app access token (HubSpot Settings → Integrations → Private Apps → Auth tab).
# Form must exist; sync property internal names with src/lib/hubspot-pre-register-fields.ts (HS).
# Add contact property bizzi_pr_creator_type (dropdown) for Photographer / Filmmaker / Content creator / Hybrid shooter.
# Add bizzi_pr_team_size (dropdown) for waitlist team size: Just Me, 1 … 10.
HUBSPOT_ACCESS_TOKEN=
HUBSPOT_PORTAL_ID=
HUBSPOT_FORM_ID=
# HubSpot form phone field API name (default matches multi-column forms, e.g. 0-2/phone). Use `phone` if your form expects the standard name.
# HUBSPOT_FORM_PHONE_FIELD_NAME=phone
# Optional override for the lead-source field value in HubSpot:
# HUBSPOT_LEAD_SOURCE=Bizzi Cloud Pre Registration
