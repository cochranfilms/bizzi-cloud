/**
 * Shared UI primitives for consistent styling across the app.
 *
 * - Button: Use for actions. Variants: primary, secondary, ghost, danger.
 * - Input: Use for single-line text. Supports error state.
 * - Modal: Centered overlay dialog with title and optional footer.
 * - SearchInput: Input with search icon; use for search fields in nav/sidebars.
 */

export { Button, type ButtonVariant } from "./Button";
export { Input } from "./Input";
export { Modal } from "./Modal";
export { SearchInput } from "./SearchInput";
export { LoadingSpinner } from "./LoadingSpinner";
export { LoadingSkeleton } from "./LoadingSkeleton";
