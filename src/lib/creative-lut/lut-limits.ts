/** Per-axis grid points for 3D LUT data (e.g. 33³, 65³ from Resolve / Apple are common). */
export const LUT_GRID_MIN = 2;
/** WebGL texture width is size²; keep within typical GL_MAX_TEXTURE_SIZE. */
export const LUT_GRID_MAX = 256;
